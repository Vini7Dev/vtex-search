<style>
    span, strong, h3 {
        font-family: Arial, Helvetica, sans-serif;
    }

    h3 {
        font-size: 45px;
    }

    .pagination-area {
        display: flex;
        flex-direction: row;
        margin: 25px 0;
    }

    .pagination-area span {
        margin: 0 5px;
    }

    .products-area {
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        align-items: center;
        justify-content: space-between;
        padding: 0 25px;
    }

    .product {
        margin: 10px;
        max-width: 250px;
    }

    .product-title {
        text-decoration: none;
        color: black;
        font-size: 18px;
    }

    .old-price {
        text-decoration: line-through;
    }

    .email-footer {
        margin-top: 45px;
    }
</style>

<div class="container">
    <h3>Codeby</h3>

    {{#if findedProducts}}
    <span>Produtos encontrados: <strong>{{totalOfProducts}}</strong> Resultado(s)</span>

    {{#if search}}
    <span>da Pesquisa por: <strong>{{search}}</strong></span>
    {{/if}}

    <span> em {{time}} ms</span>

    <div class="pagination-area">
        <span>Primeiro</span>
        <span>Anterior</span>
        <span>{{pageNumber}}</span>
        <span>Próximo</span>
        <span>Último</span>
    </div>

    <br/>
    
    <div class="products-area">
        {{#each products}}
        <div class="product">
            <span>{{productId}}</span>

            {{#each categories}}
                <span>{{this}}</span>
            {{/each}}

            <br/>

            <a href="{{link}}" class="product-title"><strong>{{productName}}</strong></a>

            <div>
                {{#if haveDiscount}}
                <span class="old-price">{{listPrice}}</span>
                {{/if}}

                <span>{{price}}</span>

                {{#if haveInstallments}}
                <span>ou {{numberOfInstallments}}x de {{installmentsValue}}</span>
                {{/if}}

                <span>Comprar</span>
            </div>
        </div>
        {{/each}}
    </div>

    <br/>

    <div class="pagination-area">
        <span>Primeiro</span>
        <span>Anterior</span>
        <span>{{pageNumber}}</span>
        <span>Próximo</span>
        <span>Último</span>
    </div>
    {{else}}
    <span>
        Sua busca não encontrou nenhum resultado.
    </span>
    {{/if}}

    <div class="email-footer">
        <span>Caso não tenha sido você quem solicitou este email, por favor desconsidere.</span>
        <br/>
        <span>Obrigado!</span>
        <br/>
        <strong>Equipe | Codeby</strong>
    </div>
</div>